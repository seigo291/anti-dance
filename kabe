local player = game.Players.LocalPlayer
local runService = game:GetService("RunService")

local spawnLocation = nil
local isTeleporting = false

-- UI作成
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = player:WaitForChild("PlayerGui")
screenGui.ResetOnSpawn = false

local function createBtn(text, pos, color)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 45, 0, 45)
    btn.Position = pos
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.SourceSansBold
    btn.Parent = screenGui
    Instance.new("UICorner").Parent = btn
    return btn
end

local setBtn = createBtn("SET", UDim2.new(1, -115, 0, 25), Color3.fromRGB(0, 100, 200))
local getBtn = createBtn("GET", UDim2.new(1, -60, 0, 25), Color3.fromRGB(60, 60, 60))

-- [SET] 処理：納品板の「真上」で保存してください
setBtn.MouseButton1Click:Connect(function()
    local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if hrp then
        spawnLocation = hrp.CFrame
        setBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
        getBtn.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
        task.wait(0.5)
        setBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 200)
    end
end)

-- [GET] 処理：叩きつけ納品
getBtn.MouseButton1Click:Connect(function()
    if isTeleporting or not spawnLocation then return end
    isTeleporting = true
    getBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    
    if hrp then
        local enemyPos = hrp.CFrame
        
        -- 1. 運ぶ対象（盗んだキャラ）を特定
        local carryModel = nil
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") and not v:IsDescendantOf(char) then
                if (v.Position - hrp.Position).Magnitude < 10 then
                    carryModel = v:FindFirstAncestorOfClass("Model")
                    if carryModel then break end
                end
            end
        end

        -- 2. 自分とキャラを納品場所へ（判定のため少し高めにワープ）
        local dropPos = spawnLocation * CFrame.new(0, 1, 0)
        hrp.CFrame = dropPos
        if carryModel and carryModel:FindFirstChild("HumanoidRootPart") then
            carryModel.HumanoidRootPart.CFrame = dropPos
        end

        -- 3. 【最重要】下方向へ強い力を加えて「納品板」に叩きつける
        task.wait(0.05) -- ワープ直後のラグ待ち
        local impactVelocity = Vector3.new(0, -50, 0)
        hrp.Velocity = impactVelocity
        if carryModel and carryModel:FindFirstChild("HumanoidRootPart") then
            carryModel.HumanoidRootPart.Velocity = impactVelocity
        end

        -- 4. 判定が受理されるまで少し待機
        task.wait(0.4)
        
        -- 5. 敵基地へ戻る
        hrp.CFrame = enemyPos
        hrp.Velocity = Vector3.new(0, 0, 0)
    end

    getBtn.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
    isTeleporting = false
end)
