local player = game.Players.LocalPlayer
local runService = game:GetService("RunService")
local noclipEnabled = false

-- UI
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = player:WaitForChild("PlayerGui")
screenGui.ResetOnSpawn = false
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 50, 0, 50)
toggleBtn.Position = UDim2.new(1, -70, 0, 30)
toggleBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
toggleBtn.Text = "PUSH"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.Parent = screenGui
Instance.new("UICorner").Parent = toggleBtn

-- 死亡回避の常時発動（可能な限り）
local function protectHumanoid()
    local char = player.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if hum then
        -- サーバー側からの死亡命令は完全には防げないが、
        -- 物理的なダメージパーツによる死亡を遅らせる
        hum:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
    end
end
player.CharacterAdded:Connect(protectHumanoid)
protectHumanoid()

-- 0.2秒の超高速壁抜け
toggleBtn.MouseButton1Click:Connect(function()
    if noclipEnabled then return end
    noclipEnabled = true
    toggleBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0) -- 実行中は赤
    
    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    local hum = char and char:FindFirstChild("Humanoid")
    
    if hrp and hum then
        -- 1. 衝突判定を消す
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
        
        -- 2. 一瞬で「壁の向こう」へ飛ばす (距離を調整: 2.0)
        -- これにより壁の中に滞在する時間を極限まで減らし、検知を避ける
        if hum.MoveDirection.Magnitude > 0 then
            hrp.CFrame = hrp.CFrame + (hum.MoveDirection * 2.0)
        else
            hrp.CFrame = hrp.CFrame + (hrp.CFrame.LookVector * 2.0)
        end
    end

    task.wait(0.2)
    
    noclipEnabled = false
    toggleBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
end)
